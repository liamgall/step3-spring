<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybbatis-3-mapper.dtd">

<mapper namespace="com.example.step3.DAO.UserDAO">

	<insert id="insertDAO" parameterType="Email">
		INSERT INTO
			user(email, postcode5, address, details, extra_info, phonenumber, password, file)
		VALUES
			("${email}", "${postcode5}", "${address}", "${details}", "${extra_info}", "${phoneNumber}", "${password}", "${uploadFile}")
	</insert>

</mapper>